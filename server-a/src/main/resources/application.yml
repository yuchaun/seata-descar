spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_a?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true
    username: root
    password: yuchuan
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
logging:
  level:
    ROOT: info
    org.mybatis: debug
mybatis:
  mapper-locations: classpath:mapper/*.xml
fescar:
  group:
    name: test_group  #fescar组名
